// app/page.tsx — React Server Component (default)
// Add "use client" only if you need interactivity or hooks.

export default function HomePage() {
  return (
    <main className="flex min-h-screen items-center justify-center">
      <p>Hello Copilot! Follow SKILL.md rules.</p>
    </main>
  );
}
