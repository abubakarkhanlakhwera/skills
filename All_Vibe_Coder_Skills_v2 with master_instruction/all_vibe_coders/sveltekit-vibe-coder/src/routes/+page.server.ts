import type { PageServerLoad } from './$types';

export const load: PageServerLoad = async () => {
  // Replace with real data fetching
  return { message: 'Hello Copilot! Follow SKILL.md rules.' };
};
